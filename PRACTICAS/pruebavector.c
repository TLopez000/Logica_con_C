#include <stdio.h>

#define max 4

typedef int Tvec[max];

void imprime(int []);
void lee(int v[]);

/*int main() {
 int v[max];
 lee(v);
 imprime(v);
 return 0;
}

void imprime(int v[]) {
 int i;
 for(i=0;i<max;i++)
    printf("%d ", v[i]);
}

void lee(int v[]) {
 int i;
 for(i=0;i<max;i++)
    scanf("%d", &(v[i]));
}
*/
